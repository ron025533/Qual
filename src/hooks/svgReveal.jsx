import gsap from "gsap";

export function svgReveal() {

  // const svgTl = gsap.timeline()

  // Example animation for the content
  
    gsap.from("#trefle", {
      yPercent: "50em",
      duration: 1,
    })
}